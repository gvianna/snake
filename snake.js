window.onload = function(){
  var stage = document.getElementById('stage');
  var context = stage.getContext('2d');

  context.fillStyle = 'black';
  context.fillStyle(0, 0, stage.width, stage.height);
}
